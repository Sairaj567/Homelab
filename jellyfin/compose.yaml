services:
  jellyfin:
    image: jellyfin/jellyfin:latest
    container_name: jellyfin

    restart: unless-stopped

    ports:
      - "8096:8096"

    devices:
      - /dev/dri:/dev/dri

    volumes:
      - ./config:/config
      - ./cache:/cache
      - /srv/media:/media

    environment:
      - TZ=Asia/Kolkata

    networks:
      - homelab

networks:
  homelab:
    external: true
